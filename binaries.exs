# Elixir supports accessing raw data as bits and bytes

# This is both archaic and useful because Elixir treats UTF-8 strings as binary

# Binary literals are enclosed between << and >>

test = <<1, 2, 3>>


